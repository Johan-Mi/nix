monitor=,preferred,auto,1.2

xwayland:force_zero_scaling = true

$terminal = alacritty
$menu = rofi -show run

exec-once = hyprctl setcursor "Vanilla-DMZ" 24 & swaybg -i ~/Pictures/wallpapers/smokayyy.jpg & waybar & warn-battery

general {
    gaps_in = 0
    gaps_out = 0

    col.active_border = rgb(777777)
    col.inactive_border = rgb(000000)

    layout = master
}

decoration:shadow:enabled = false
decoration:blur:enabled = false
animations:enabled = false

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]

master {
    new_status = master
}

input {
    kb_layout = se_tweaks
    kb_variant = nodeadkeys
    kb_options = compose:prsc,compose:menu
    repeat_delay = 300
    repeat_rate = 40

    touchpad {
        disable_while_typing = false
        tap-to-click = false
    }
}

binds {
    allow_workspace_cycles = true
}

$mainMod = SUPER

bind = $mainMod, return, exec, $terminal
bind = $mainMod, I, exec, firefox --new-window
bind = $mainMod SHIFT, I, exec, firefox --private-window
bind = CTRL ALT, L, exec, swaylock --color 000000
bind = $mainMod SHIFT, C, killactive,
bind = $mainMod SHIFT, Q, exit,
bind = $mainMod, space, togglefloating,
bind = $mainMod, P, exec, $menu
bind = $mainMod, F, fullscreen, 1
bind = $mainMod, tab, workspace, previous
bind = $mainMod, B, exec, pkill -USR1 waybar

bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bindel = $mainMod ALT, W, exec, change-volume 1%+
bindel = $mainMod ALT, Q, exec, change-volume 1%-
bindel = $mainMod ALT, S, exec, brightnessctl set 1%+
bindel = $mainMod ALT, A, exec, brightnessctl set 1%-
bindl = $mainMod SHIFT, O, exec, brightnessctl set 0

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
